export const SERVER_IP = process.env.REACT_APP_API_BASE_URL;

export const API_URL = `${SERVER_IP}:8090`;
export const PUBLIC_URL = `${SERVER_IP}:3001`;
export const LOGIN_MS_URL = `${SERVER_IP}:3000`;

// Account Service
export const ACCOUNT_URL = `${SERVER_IP}:8100`;

// Indusrty Service
export const INDUSTRY_URL = `${SERVER_IP}:8200`;

// Address Service
export const ADDRESS_URL = `${SERVER_IP}:8300`;

// Document Service
export const DOCUMENT_URL = `${SERVER_IP}:8500`;

// Geo, Currency Service
export const GEO_URL = `${SERVER_IP}:8600`;

// Contact Service
export const CONTACT_URL = `${SERVER_IP}:8700`;

// Account Instruction Service
export const ACCOUNT_INSTRUCTION_URL = `${SERVER_IP}:8800`;

// Department Service
export const DEPARTMENT_URL = `${SERVER_IP}:8900`;

// Role Service
export const ACCESS_URL = `${SERVER_IP}:8091`;

// Group Service
export const GROUP_URL = `${SERVER_IP}:8092`;

// Candidate Service
export const CANDIDATE_URL = `${SERVER_IP}:8050`;

// Job Service
export const JOB_URL = `${SERVER_IP}:9200`;

// Form Service
export const FORM_URL = `${SERVER_IP}:9400`;

// Candidate/Work experience service
export const CANDIDATE_WORK_EXPERIENCE_URL = `${SERVER_IP}:9500`;

// Candidate/Education service
export const CANDIDATE_EDUCATION_DETAILS_URL = `${SERVER_IP}:9600`;

// Candidate/Certificate service
export const CANDIDATE_CERTIFICATE_URL = `${SERVER_IP}:9700`;

// Candidate/Language service
export const CANDIDATE_LANGUAGES_URL = `${SERVER_IP}:9800`;

// Candidate/Employer details service
export const CANDIDATE_EMPLOYER_DETAILS_URL = `${SERVER_IP}:9900`;

// Common Service (Template, Email, SMS)
export const COMMON_URL = `${SERVER_IP}:8181`;

export const DASHBOARD_URL = `${SERVER_IP}:9300`;

// 



